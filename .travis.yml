language: node_js

node_js:
  - '8.9.4'

cache:
  yarn: true
  directories:
    - node_modules

script:
  - yarn lint
  - yarn build

deploy:
  provider: surge
  skip_cleanup: true
  project: ./dist/
  domain: gestao-paroquial.surge.sh
